# Test Database (use a different database for testing)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/shadowme_test?schema=public"

# JWT configuration (use different secrets for testing)
JWT_SECRET="test-jwt-secret-key"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_SECRET="test-refresh-token-secret"
JWT_REFRESH_EXPIRES_IN="7d"

# Server configuration
PORT=3001